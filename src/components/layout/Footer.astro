---
// src/components/layout/Footer.astro
// 功能：网站底部 — 紧凑单行底栏：版权信息、协议链接、社交图标
import { getLangFromUrl, getLocalizedPath } from '@/i18n/utils';
import { siteConfig } from '@/config/site';

const lang = getLangFromUrl(Astro.url);
const year = siteConfig.copyright.year;

function getLabel(item: { zh?: string; en?: string }): string {
  return lang === 'en' ? item.en || item.zh || '' : item.zh || item.en || '';
}
---

<footer class="border-t border-border bg-background">
  <div
    class="mx-auto flex max-w-7xl flex-col items-center gap-4 px-4 py-4 md:flex-row md:justify-between"
  >
    {/* Left: Copyright + Protocol links */}
    <div class="flex flex-wrap items-center justify-center gap-3 text-xs text-text-disabled">
      <span>&copy; {year} {siteConfig.copyright.holder}</span>
      {
        siteConfig.protocols.map((proto) => (
          <>
            <span class="bottom-separator hidden sm:inline" />
            <a
              href={getLocalizedPath(proto.path, lang)}
              class="footer-link transition-colors duration-150 hover:text-text-secondary"
            >
              {getLabel(proto)}
            </a>
          </>
        ))
      }
    </div>

    {/* Right: Social icons */}
    <div class="flex items-center gap-2">
      {
        siteConfig.social.map((link) => (
          <a
            href={link.href}
            target="_blank"
            rel="noopener noreferrer"
            class="social-btn flex h-8 w-8 items-center justify-center rounded-md text-text-disabled transition-colors duration-150 hover:text-text-primary"
            aria-label={link.label}
          >
            {link.icon === 'github' && (
              <svg class="h-4 w-4" fill="currentColor" viewBox="0 0 24 24">
                <path
                  fill-rule="evenodd"
                  d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z"
                  clip-rule="evenodd"
                />
              </svg>
            )}
            {link.icon === 'twitter' && (
              <svg class="h-4 w-4" fill="currentColor" viewBox="0 0 24 24">
                <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z" />
              </svg>
            )}
          </a>
        ))
      }
    </div>
  </div>
</footer>

<style>
  .social-btn:focus-visible {
    box-shadow:
      0 0 0 2px var(--color-background, #fff),
      0 0 0 4px #006bff;
    outline: none;
  }

  .footer-link:focus-visible {
    box-shadow:
      0 0 0 2px var(--color-background, #fff),
      0 0 0 4px #006bff;
    outline: none;
    border-radius: 4px;
  }

  .bottom-separator {
    display: inline-block;
    width: 1px;
    height: 12px;
    background-color: var(--color-border, #eaeaea);
    vertical-align: middle;
  }
</style>
